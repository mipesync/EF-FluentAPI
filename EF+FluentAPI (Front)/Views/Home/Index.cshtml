@model List<Product>

@{
    ViewData["Title"] = "Главная";
}

<h1>Продукты</h1>

<table class="table">
    <thead>
    <tr>
        <th>
            <a>Наименование</a>
        </th>
        <th>
            <a>Цена</a>
        </th>
        <th></th>
    </tr>
    </thead>
    @foreach (var product in Model)
    {
        <tbody>
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => product.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => product.Price)
                </td>
                <td>
                    <a asp-controller="Cart" asp-action="AddToCart" asp-route-id="@product.Id">Добавить в заказ</a>
                </td>
            </tr>
        </tbody>
    }
</table>
